package org.powershell.preferences;

import org.eclipse.swt.graphics.RGB;

/**
 * The Interface IPSColorConstants.
 * 
 * @author dalexandrov
 */
public interface IPSColorConstants {

	/** The COMMENT. */
	RGB COMMENT = new RGB(0, 128, 0);

	/** The STRING. */
	RGB STRING = new RGB(0, 128, 255);

	/** The VARIABLE. */
	RGB VARIABLE = new RGB(200, 50, 50);

	/** The LANGUAG e_ reserved. */
	RGB LANGUAGE_RESERVED = new RGB(128, 0, 64);

	/** The DAS h_ operator. */
	RGB DASH_OPERATOR = new RGB(255, 0, 64);

	/** The CMDLET. */
	RGB CMDLET = new RGB(0, 0, 128);

	/** The KNOW n_ func. */
	RGB KNOWN_FUNC = new RGB(107, 142, 35);

	/** The DEFAULT. */
	RGB DEFAULT = new RGB(0, 0, 0);
}
